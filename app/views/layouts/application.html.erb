<!DOCTYPE html>
<html>
  <head>
    <title>Transit App</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Zilla+Slab" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  </head>

  <body>
    <nav>
      <div class="nav-wrapper">
        <div class="brand-logo"><%= link_to 'Caltrain', stations_path %></div>
        <% if signed_in? %>
          <ul id="dropdown1" class="dropdown-content">
            <% @favorites.each do |fav| %>
              <li><a data-from-station-id="1" data-to-station-id="5"><%= fav.source %> to <%= fav.destination %></a></li>
            <% end %>
          </ul>
          <ul id="nav-mobile" class="right">
            <li><%= current_user.email %></li>
            <li><a class="dropdown-button" data-activates="dropdown1">Favorite Routes<i class="material-icons right">arrow_drop_down</i></a></li>
            <li><%= link_to 'Sign out', sign_out_path, method: :delete, class: 'btn-small waves-effect waves-light right' %></li>
          </ul>
        <% else %>
          <ul id="nav-mobile" class="right">
            <li><%= link_to 'Log In', sign_in_path, class: 'btn-small waves-effect waves-light right' %></li>
            <li><%= link_to 'Sign up', sign_up_path, class: 'btn-small waves-effect waves-light right' %></li>
          </ul>
        <% end %>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
